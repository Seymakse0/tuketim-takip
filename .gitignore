node_modules
# .next — yalnızca routes.d.ts izlensin (Next build çıktısının tamamı repoya girmesin)
.next/*
!.next/types/
.next/types/*
!.next/types/routes.d.ts
out
.env
.env.local
# Yerel port çakışması için; yanlışlıkla commit edilirse üretimde Nginx (3005) ile uyumsuzluk → 502
docker-compose.override.yml
.env.*.local
npm-debug.log*
.DS_Store
*.pem
