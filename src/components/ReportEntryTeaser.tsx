/**
 * Eski sürümde kullanılıyordu; ana sayfa artık bu bileşeni import etmiyor.
 * IDE’de açık kalan sekmeler / yerel kopyalar için bırakıldı; güvenle silebilirsiniz.
 */
export function ReportEntryTeaser(): null {
  return null;
}
